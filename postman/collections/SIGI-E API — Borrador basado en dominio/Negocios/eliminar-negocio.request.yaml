$kind: http-request
name: 'Eliminar negocio'
description: |-
  Placeholder de borrador para eliminar un negocio usando su identificador.

  Request documental basado en el dominio, no en un endpoint implementado confirmado.
method: DELETE
url: '{{base_url}}{{api_prefix}}{{api_version}}/negocios/{{negocio_id}}'
order: 5000
headers:
  - key: Authorization
    value: 'Bearer {{auth_token}}'
