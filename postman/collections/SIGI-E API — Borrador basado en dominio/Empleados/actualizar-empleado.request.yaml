$kind: http-request
name: 'Actualizar empleado'
description: |-
  Placeholder de borrador para actualizar información de un empleado.

  Request estimado a partir del modelo de dominio.
  No ha sido validado contra una API implementada.
method: PUT
url: '{{base_url}}{{api_prefix}}{{api_version}}/empleados/{{empleado_id}}'
order: 4000
headers:
  - key: Content-Type
    value: application/json
  - key: Authorization
    value: 'Bearer {{auth_token}}'
body:
  type: json
  content: |-
    {
      "nombre": "Empleado actualizado",
      "especialidad": "Colorimetría"
    }
